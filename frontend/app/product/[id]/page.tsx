import React from "react";
import ProductPage from "../../_pages/product";

const IndivProduct = ({ params }: { params: { id: string } }) => {
  return <ProductPage />;
};

export default ProductPage;
